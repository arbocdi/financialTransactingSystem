spring.application.name=tests
# ===============================
# = services
# ===============================
accounts.base-url=${accounts.address}/api/accounts
transfers.base-url=${transfers.address}/api/transfers
